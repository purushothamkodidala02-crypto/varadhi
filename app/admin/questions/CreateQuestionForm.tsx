"use client";

import { useActionState, useMemo, useState } from "react";
import { createQuestion, type CreateQuestionState } from "./actions";
import type { QuestionLifecycle } from "@/types/question";

type Category = { id: string; name: string }; type Exam = { id: string; categoryId: string; name: string }; type Paper = { id: string; examId: string; name: string }; type Subject = { id: string; paperId: string; name: string };
const initialState: CreateQuestionState = { success: false, message: "" };
export function CreateQuestionForm({ categories, exams, papers, subjects }: { categories: Category[]; exams: Exam[]; papers: Paper[]; subjects: Subject[] }) {
  const [state, action, pending] = useActionState(createQuestion, initialState); const [categoryId, setCategoryId] = useState(""); const [examId, setExamId] = useState(""); const [paperId, setPaperId] = useState(""); const [lifecycle, setLifecycle] = useState<QuestionLifecycle>("permanent");
  const visibleExams = useMemo(() => exams.filter((exam) => exam.categoryId === categoryId), [exams, categoryId]); const visiblePapers = useMemo(() => papers.filter((paper) => paper.examId === examId), [papers, examId]); const visibleSubjects = useMemo(() => subjects.filter((subject) => subject.paperId === paperId), [subjects, paperId]);
  return <section className="mt-8 overflow-hidden rounded-3xl border bg-white shadow-sm"><div className="border-b bg-slate-50 px-7 py-5"><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700">New question</p><h2 className="mt-2 text-2xl font-black">Classify, then add the question</h2><p className="mt-2 text-sm text-slate-600">Every question belongs to one Exam Category, Exam, Paper, and Subject. It can be reused in any mock test for that Subject or Paper.</p></div><form action={action} className="p-6 sm:p-7"><div className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><section className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700">1. Question location</p><div className="mt-4 space-y-4"><label className="block text-sm font-bold">Exam Category<select name="exam_id" required value={categoryId} onChange={(event) => { setCategoryId(event.target.value); setExamId(""); setPaperId(""); }} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="">Choose category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-bold">Exam<select name="exam_group_id" required disabled={!categoryId} value={examId} onChange={(event) => { setExamId(event.target.value); setPaperId(""); }} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Exam</option>{visibleExams.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-bold">Paper<select name="paper_id" required disabled={!examId} value={paperId} onChange={(event) => setPaperId(event.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Paper</option>{visiblePapers.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-bold">Subject<select name="subject_id" required disabled={!paperId} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Subject</option>{visibleSubjects.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label></div><label className="mt-6 flex items-center gap-3 rounded-xl border bg-white p-3 text-sm font-bold"><input name="is_active" type="checkbox" defaultChecked className="h-4 w-4" />Ready to use in mock tests</label><label className="mt-5 block text-sm font-bold">Question lifetime<select name="content_lifecycle" value={lifecycle} onChange={(event) => setLifecycle(event.target.value as QuestionLifecycle)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="permanent">Permanent</option><option value="review">Review later</option><option value="expires">Expire after a date</option></select></label>{lifecycle === "review" && <label className="mt-4 block text-sm font-bold">Review on<input name="review_on" type="date" required className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label>}{lifecycle === "expires" && <label className="mt-4 block text-sm font-bold">Stop using after<input name="expires_on" type="date" required className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label>}</section><section><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700">2. Question content</p><label className="mt-4 block text-sm font-bold">Question<textarea name="question_text" required rows={4} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal leading-6" /></label><div className="mt-5 grid gap-4 sm:grid-cols-2">{["A", "B", "C", "D"].map((letter) => <label key={letter} className="block text-sm font-bold">Option {letter}<input name={`option_${letter.toLowerCase()}`} required className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label>)}</div><label className="mt-5 block text-sm font-bold">Correct answer<select name="correct_answer" required defaultValue="" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="" disabled>Choose option</option>{["A", "B", "C", "D"].map((letter) => <option key={letter} value={letter}>Option {letter}</option>)}</select></label><details className="mt-6 rounded-xl border p-4"><summary className="cursor-pointer text-sm font-bold">Explanation and source (optional)</summary><label className="mt-4 block text-sm font-bold">Explanation<textarea name="explanation" rows={3} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="mt-4 block text-sm font-bold">Source reference<input name="source_reference" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label></details></section></div><div className="mt-6"><button disabled={pending || !paperId} className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{pending ? "Saving..." : "Save Question"}</button>{state.message && <p className={`mt-4 text-sm font-semibold ${state.success ? "text-emerald-700" : "text-red-700"}`}>{state.message}</p>}</div></form></section>;
}
