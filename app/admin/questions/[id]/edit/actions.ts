"use server";

import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import type { CorrectAnswer, QuestionLifecycle } from "@/types/question";

export type UpdateQuestionState = { success: boolean; message: string };
const answers: CorrectAnswer[] = ["A", "B", "C", "D"]; const lifecycles: QuestionLifecycle[] = ["permanent", "review", "expires"];
const validDate = (value: string) => /^\d{4}-\d{2}-\d{2}$/.test(value) && !Number.isNaN(Date.parse(`${value}T00:00:00Z`));
export async function updateQuestion(questionId: string, _previous: UpdateQuestionState, formData: FormData): Promise<UpdateQuestionState> { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return { success: false, message: "You must be logged in." }; const { data: profile } = await supabase.from("profiles").select("role").eq("id", user.id).single(); if (profile?.role !== "admin") return { success: false, message: "You are not authorized to update Questions." }; const subjectId = String(formData.get("subject_id") ?? "").trim(); const questionText = String(formData.get("question_text") ?? "").trim(); const options = ["a", "b", "c", "d"].map((letter) => String(formData.get(`option_${letter}`) ?? "").trim()); const correctAnswer = String(formData.get("correct_answer") ?? "") as CorrectAnswer; const lifecycle = String(formData.get("content_lifecycle") ?? "permanent") as QuestionLifecycle; const reviewOn = String(formData.get("review_on") ?? "").trim(); const expiresOn = String(formData.get("expires_on") ?? "").trim(); if (!subjectId || !questionText || options.some((item) => !item) || !answers.includes(correctAnswer)) return { success: false, message: "Complete the Subject, Question, options, and correct answer." }; if (new Set(options.map((item) => item.toLowerCase())).size !== 4) return { success: false, message: "All four answer options must be different." }; if (!lifecycles.includes(lifecycle) || (lifecycle === "review" && !validDate(reviewOn)) || (lifecycle === "expires" && !validDate(expiresOn))) return { success: false, message: "Choose a valid question lifetime and date." }; const { data: subject } = await supabase.from("subjects").select("id").eq("id", subjectId).maybeSingle(); if (!subject) return { success: false, message: "The selected Subject could not be found." }; const { data: duplicate } = await supabase.from("questions").select("id").eq("subject_id", subjectId).eq("question_text", questionText).neq("id", questionId).maybeSingle(); if (duplicate) return { success: false, message: "This Question already exists under the selected Subject." }; const { error } = await supabase.from("questions").update({ subject_id: subjectId, question_text: questionText, option_a: options[0], option_b: options[1], option_c: options[2], option_d: options[3], correct_answer: correctAnswer, explanation: String(formData.get("explanation") ?? "").trim() || null, source_reference: String(formData.get("source_reference") ?? "").trim() || null, image_url: String(formData.get("image_url") ?? "").trim() || null, is_active: formData.get("is_active") === "on", content_lifecycle: lifecycle, review_on: lifecycle === "review" ? reviewOn : null, expires_on: lifecycle === "expires" ? expiresOn : null }).eq("id", questionId); if (error) return { success: false, message: error.message }; revalidatePath("/admin/questions"); revalidatePath(`/admin/questions/${questionId}/edit`); revalidatePath("/admin/mock-tests"); return { success: true, message: "Question updated successfully." }; }
