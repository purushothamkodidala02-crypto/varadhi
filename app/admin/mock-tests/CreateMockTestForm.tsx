"use client";

import { useActionState, useMemo, useState } from "react";
import { createMockTest, type CreateMockTestState } from "./actions";

type Category = { id: string; name: string }; type Exam = { id: string; categoryId: string; name: string }; type Paper = { id: string; examId: string; name: string; duration: number | null }; type Subject = { id: string; paperId: string; name: string };
const initialState: CreateMockTestState = { success: false, message: "" };
export function CreateMockTestForm({ categories, exams, papers, subjects }: { categories: Category[]; exams: Exam[]; papers: Paper[]; subjects: Subject[] }) { const [state, action, pending] = useActionState(createMockTest, initialState); const [categoryId, setCategoryId] = useState(""); const [examId, setExamId] = useState(""); const [paperId, setPaperId] = useState(""); const [scope, setScope] = useState<"paper" | "subject">("paper"); const [accessType, setAccessType] = useState("free"); const availableExams = useMemo(() => exams.filter((item) => item.categoryId === categoryId), [exams, categoryId]); const availablePapers = useMemo(() => papers.filter((item) => item.examId === examId), [papers, examId]); const availableSubjects = useMemo(() => subjects.filter((item) => item.paperId === paperId), [subjects, paperId]); const selectedPaper = papers.find((item) => item.id === paperId); return <section className="mt-8 rounded-3xl border bg-white p-6 shadow-sm sm:p-7"><p className="text-xs font-bold uppercase tracking-[0.14em] text-teal-700">New mock test</p><h2 className="mt-2 text-2xl font-black">Create a Paper-wise or Subject-wise mock</h2><form action={action} className="mt-6 grid gap-5 md:grid-cols-2"><label className="block text-sm font-bold">Exam Category<select name="exam_id" required value={categoryId} onChange={(event) => { setCategoryId(event.target.value); setExamId(""); setPaperId(""); }} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="">Choose category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-bold">Exam<select name="exam_group_id" required disabled={!categoryId} value={examId} onChange={(event) => { setExamId(event.target.value); setPaperId(""); }} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Exam</option>{availableExams.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="block text-sm font-bold">Paper<select name="paper_id" required disabled={!examId} value={paperId} onChange={(event) => setPaperId(event.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Paper</option>{availablePapers.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><fieldset className="rounded-xl border p-4"><legend className="px-1 text-sm font-bold">Mock type</legend><label className="mt-1 flex gap-2 text-sm"><input name="test_scope" type="radio" value="paper" checked={scope === "paper"} onChange={() => setScope("paper")} />Paper-wise full-length mock</label><label className="mt-2 flex gap-2 text-sm"><input name="test_scope" type="radio" value="subject" checked={scope === "subject"} onChange={() => setScope("subject")} />Subject-wise mock</label></fieldset>{scope === "subject" && <label className="block text-sm font-bold md:col-span-2">Subject<select name="subject_id" required disabled={!paperId} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal disabled:bg-slate-100"><option value="">Choose Subject</option>{availableSubjects.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label>}<label className="block text-sm font-bold">Mock Test title<input name="title" required defaultValue="Mock Test 01" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Slug<input name="slug" required pattern="[a-z0-9-]+" defaultValue="mock-test-01" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold md:col-span-2">Description<textarea name="description" rows={3} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Duration in minutes<input name="duration_minutes" type="number" min="1" required defaultValue={selectedPaper?.duration ?? 60} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Display order<input name="display_order" type="number" min="0" required defaultValue="1" className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Student access<select name="access_type" value={accessType} onChange={(event) => setAccessType(event.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="free">Free</option><option value="paid">Paid</option></select></label>{accessType === "paid" && <label className="block text-sm font-bold">Price in ₹<input name="price_inr" type="number" min="1" step="0.01" required className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label>}<div className="md:col-span-2"><button disabled={pending || !paperId} className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{pending ? "Creating..." : "Create draft mock test"}</button>{state.message && <p className={`mt-4 text-sm font-semibold ${state.success ? "text-emerald-700" : "text-red-700"}`}>{state.message}</p>}</div></form></section>; }
