"use client";

import { useActionState } from "react";
import { createPaper, updatePaper, type PaperActionState } from "./actions";

export type ExamOption = { id: string; label: string };
type PaperValues = { id?: string; exam_group_id: string; name: string; slug: string; description: string | null; duration_minutes: number | null; question_count: number | null; default_correct_marks: number; default_negative_marks: number; display_order: number; is_active: boolean };
const initialState: PaperActionState = { success: false, message: "" };

export function PaperForm({ exams, paper }: { exams: ExamOption[]; paper?: PaperValues }) {
  const action = paper ? updatePaper : createPaper;
  const [state, formAction, pending] = useActionState(action, initialState);

  return <section className="mt-8 rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">{paper ? "Edit Paper" : "Add Paper"}</h2><p className="mt-1 text-sm text-slate-600">A Paper represents one government paper and sets the default scoring for its questions.</p><form action={formAction} className="mt-6 grid gap-5 md:grid-cols-2">{paper?.id && <input type="hidden" name="paper_id" value={paper.id} />}<label className="block text-sm font-bold">Exam<select name="exam_group_id" required defaultValue={paper?.exam_group_id ?? exams[0]?.id} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal"><option value="">Choose an Exam</option>{exams.map((exam) => <option key={exam.id} value={exam.id}>{exam.label}</option>)}</select></label><label className="block text-sm font-bold">Paper name<input name="name" required defaultValue={paper?.name ?? "Paper 1 - General Studies"} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Slug<input name="slug" required pattern="[a-z0-9-]+" defaultValue={paper?.slug ?? "paper-1-general-studies"} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Display order<input name="display_order" type="number" min="0" required defaultValue={paper?.display_order ?? 1} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Duration in minutes <span className="font-normal text-slate-500">(optional)</span><input name="duration_minutes" type="number" min="1" defaultValue={paper?.duration_minutes ?? ""} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Question count <span className="font-normal text-slate-500">(optional)</span><input name="question_count" type="number" min="1" defaultValue={paper?.question_count ?? ""} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Correct marks per question<input name="default_correct_marks" type="number" min="0.01" step="0.01" required defaultValue={paper?.default_correct_marks ?? 1} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold">Negative marks per wrong answer<input name="default_negative_marks" type="number" min="0" step="0.01" required defaultValue={paper?.default_negative_marks ?? 0} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="block text-sm font-bold md:col-span-2">Description<textarea name="description" rows={3} defaultValue={paper?.description ?? ""} className="mt-2 w-full rounded-xl border px-4 py-3 font-normal" /></label><label className="flex items-center gap-3 text-sm font-bold md:col-span-2"><input name="is_active" type="checkbox" defaultChecked={paper?.is_active ?? true} className="h-4 w-4" />Available to students</label><div className="md:col-span-2"><button disabled={pending || exams.length === 0} className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{pending ? "Saving..." : paper ? "Save Paper" : "Add Paper"}</button>{state.message && <p className={`mt-4 text-sm font-semibold ${state.success ? "text-emerald-700" : "text-red-700"}`}>{state.message}</p>}</div></form></section>;
}
